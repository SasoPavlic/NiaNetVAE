model_params:
  name: 'LSTMVAE'
  seq_len: 140
  n_features: 1
  embedding_dim: 128

data_params:
  data_path: "data/"
  train_batch_size: 1
  test_batch_size: 1
  val_batch_size: 1
  patch_size: 0
  num_workers: 16

exp_params:
  LR: 0.001
  weight_decay: 0.0
  kld_weight: 0.1
  manual_seed: 1265

trainer_params:
  gpus: [ 0 ]
  max_epochs: 150

logging_params:
  save_dir: "logs/"
  name: "LSTMVAE"

